//Bubble Left scales based on txt 
document.addEventListener("DOMContentLoaded", function () {
    var bubble = document.getElementById('bubble-mob-scale');
    var textLength = bubble.innerText.length;

    // Adjust the font size based on the text length
    if (textLength > 100) {
        bubble.style.fontSize = '11px';
    } else if (textLength > 50) {
        bubble.style.fontSize = '12px';
    } else {
        bubble.style.fontSize = '14px';
    }
});

// Bubble Charge scales based on txt

document.addEventListener("DOMContentLoaded", function () {
    var bubble = document.getElementById('bubble-chg-scale');
    var textLength = bubble.innerText.length;

    // Adjust the font size based on the text length
    if (textLength > 100) {
        bubble.style.fontSize = '11px';
    } else if (textLength > 50) {
        bubble.style.fontSize = '12px';
    } else {
        bubble.style.fontSize = '14px';
    }
});


// Bubble right scales based on txt 

document.addEventListener("DOMContentLoaded", function () {
    var bubble = document.getElementById('bubble-right-scale');
    var textLength = bubble.innerText.length;

    // Adjust the font size based on the text length
    if (textLength > 100) {
        bubble.style.fontSize = '12px';
    } else if (textLength > 24) {
        bubble.style.fontSize = '14px';
    } else {
        bubble.style.fontSize = '16px';
    }
});

// Bubble auc scales based on txt 

document.addEventListener("DOMContentLoaded", function () {
    var bubble = document.getElementById('bubble-auc-scale');
    var textLength = bubble.innerText.length;

    // Adjust the font size based on the text length
    if (textLength > 100) {
        bubble.style.fontSize = '12px';
    } else if (textLength > 50) {
        bubble.style.fontSize = '14px';
    } else {
        bubble.style.fontSize = '16px';
    }
});

// Bubble auc scales based on txt 

document.addEventListener("DOMContentLoaded", function () {
    var bubble = document.getElementById('obit-php');
    var textLength = bubble.innerText.length;

    // Adjust the font size based on the text length
    if (textLength > 100) {
        bubble.style.fontSize = '12px';
    } else if (textLength > 50) {
        bubble.style.fontSize = '14px';
    } else {
        bubble.style.fontSize = '16px';
    }
});

// Bubble auc scales based on txt 

document.addEventListener("DOMContentLoaded", function () {
    var bubble = document.getElementById('top-caption');
    var textLength = bubble.innerText.length;

    // Adjust the font size based on the text length
    if (textLength > 100) {
        bubble.style.fontSize = '11px';
    } else if (textLength > 50) {
        bubble.style.fontSize = '12px';
    } else {
        bubble.style.fontSize = '14px';
    }
});

// Bubble auc scales based on txt 

document.addEventListener("DOMContentLoaded", function () {
    var bubble = document.getElementById('bottom-caption');
    var textLength = bubble.innerText.length;

    // Adjust the font size based on the text length
    if (textLength > 100) {
        bubble.style.fontSize = '11px';
    } else if (textLength > 50) {
        bubble.style.fontSize = '12px';
    } else {
        bubble.style.fontSize = '14px';
    }
});